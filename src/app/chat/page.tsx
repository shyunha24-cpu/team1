"use client";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
type Message={id:number;sender:"me"|"friend";text:string;time:string};
const initial:Message[]=[{id:1,sender:"friend",text:"나 이제 지하철 탔어! 성수역에서 보자 ☻",time:"17:56"},{id:2,sender:"me",text:"좋아, 나도 준비 거의 끝났어.",time:"17:58"}];
export default function Chat(){const[messages,setMessages]=useState<Message[]>([]),[draft,setDraft]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{try{const saved=localStorage.getItem("momentum-messages");setMessages(saved?JSON.parse(saved):initial)}catch{setError("대화를 불러오지 못했어요. 다시 시도해 주세요.")}const t=setTimeout(()=>setLoading(false),450);return()=>clearTimeout(t)},[]);const send=(e:FormEvent)=>{e.preventDefault();const text=draft.trim();if(!text)return;const next=[...messages,{id:Date.now(),sender:"me" as const,text,time:new Date().toLocaleTimeString("ko-KR",{hour:"2-digit",minute:"2-digit",hour12:false})}];try{localStorage.setItem("momentum-messages",JSON.stringify(next));setMessages(next);setDraft("")}catch{setError("메시지를 보내지 못했어요. 다시 시도해 주세요.")}};return <main className="shell"><nav className="topbar"><Link href="/" className="brand">← Momentum</Link><span className="avatar">민</span></nav><section className="hero glass"><div className="eyebrow"><span className="live-dot"/>성수 모임 · 3명 연결됨</div><h1>서연이네<br/><em>생일 모임</em></h1><p>오늘 18:30 · 성수역 3번 출구</p></section><section className="section-heading"><div><p>그룹 대화</p><h2>친구들과 조율하기</h2></div><span className="progress">32분 남음</span></section>{loading?<section className="tip glass"><div className="spinner"/><p>대화를 불러오는 중이에요.</p></section>:error?<section className="tip glass"><p>{error}</p><button onClick={()=>{setError("");setMessages(initial)}}>다시 시도</button></section>:messages.length===0?<section className="tip glass"><p><strong>아직 대화가 없어요.</strong><br/>먼저 친구에게 말을 건네보세요.</p></section>:<section className="routine-list glass">{messages.map(m=><article className="routine-item" key={m.id}><span className={`check ${m.sender==="me"?"done":""}`}>{m.sender==="me"?"나":"친"}</span><span className="routine-copy"><strong>{m.text}</strong><small>{m.time}</small></span></article>)}</section>}<section className="tip glass"><span className="tip-icon">✦</span><p><strong>준비 완료를 알려주세요</strong><br/>친구들이 내 진행 상황을 볼 수 있어요.</p></section><form onSubmit={send} className="chat-form"><input value={draft} onChange={e=>setDraft(e.target.value)} placeholder="메시지 입력" aria-label="메시지 입력"/><button type="submit" disabled={!draft.trim()}>보내기</button></form></main>}
